<!DOCTYPE html>
<html>
<head>
  <title>WhatsApp Bot</title>
  <style>
    body {
      display: flex; flex-direction: column;
      align-items: center; justify-content: center;
      height: 100vh; margin: 0;
      font-family: Arial;
      text-align: center;
    }
    button {
      margin: 10px;
      padding: 10px 20px;
    }
  </style>
</head>
<body>
  <% if (!isAuthenticated && qr) { %>
    <img src="<%= qr %>" alt="QR Code" />
  <% } else if (isAuthenticated) { %>
    <p>✅ Sudah Login</p>
  <% } %>

  <form action="/restart-session" method="POST">
    <button><%= isAuthenticated ? 'Login Ulang' : 'Login' %></button>
  </form>

  <button onclick="fetchUsers()">Tampilkan User</button>
  <button onclick="fetchLog()">Tampilkan Log</button>

  <pre id="output" style="max-height: 300px; overflow-y: auto;"></pre>

  <script src="/script.js"></script>
</body>
</html>
